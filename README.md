# nest.mattrossman.com

Static site for a private personal integration.

## GitHub Pages

In **Settings → Pages**, choose **Deploy from a branch**, select `main` and `/ (root)`, then configure the custom domain `nest.mattrossman.com`.
